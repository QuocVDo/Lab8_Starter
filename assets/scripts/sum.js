/*
 * According to  Jaime, we need to create our own sum.js and fill it with it our own function
 */


// Export command to export the right functions
module.exports = {sum};

//Function to take 2 numbers and add them
function sum(x,y) {
    return x + y; 
}